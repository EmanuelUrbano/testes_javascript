/* função transformar array de arrays em obj */
//recebe array de arrays

//le primeira linha(será o nome dos campos)
function transformarArrayDeArraysEmArrayDeObjetos(arrayDeArrays){
    //pega o nome de cada elemento no primero array  e sua posição, se nos proximos arrays o elemento estiver na mesma posição será o campo certo
    arrayDeArrays.forEach(element => {
        
    });
}
